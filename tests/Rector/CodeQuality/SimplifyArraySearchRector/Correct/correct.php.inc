<?php declare(strict_types=1);

if (in_array('searching', $array)) {
    echo 'yes!';
}

if (in_array('searching', $array)) {
    echo 'yes!';
}

if (!in_array('searching', $array)) {
    echo 'no!';
}

if (!in_array('searching', $array)) {
    echo 'no!';
}
