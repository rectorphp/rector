<?php declare(strict_types=1);

while (list($module) = each($module_list)) {
}

while (list($key, $callback) = each($callbacks)) {
    // comment
}

$module_list = ['a', 'b'];

while (list($module) = each($module_list)) {
    echo $module;
}
