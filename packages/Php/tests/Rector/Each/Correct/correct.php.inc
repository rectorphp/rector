<?php declare(strict_types=1);

foreach (array_keys($module_list) as $module) {
}

foreach ($callbacks as $key => $callback) {
    // comment
}

$module_list = ['a', 'b'];

foreach (array_keys($module_list) as $module) {
    echo $module;
}
