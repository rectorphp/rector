<?php declare(strict_types=1);

if (in_array('searching', $array, true)) {
    echo 'yes!';
}

if (in_array('searching', $array, true)) {
    echo 'yes!';
}

if (!in_array('searching', $array, true)) {
    echo 'no!';
}

if (!in_array('searching', $array, true)) {
    echo 'no!';
}
