language: php

php:
  - 7.1

install:
  - composer install
  # prepare nette/sandbox
  - composer create-project nette/sandbox:@dev temp/nette-sandbox
  # prepare symfony/symfony-demo
  - composer create-project symfony/symfony-demo:@dev temp/symfony-demo

script:
  - vendor/bin/phpunit --coverage-clover coverage.xml
  # check coding standard (defined in composer.json "scripts" section)
  # if this fails, run "composer fix-cs" to fix all fixable issues
  - composer check-cs
  # check with phpstan (defined in composer.json "scripts" section)
  - composer phpstan
  # try on nette/sandbox
  - bin/rector process temp/nette-sandbox --config src/config/level/nette/all.yml
  # try on symfony/symfony-demo
  - bin/rector process temp/symfony-demo --config src/config/level/symfony/all.yml

after_script:
  # upload coverage to Coveralls.io
  - wget https://github.com/satooshi/php-coveralls/releases/download/v1.0.1/coveralls.phar
  - php coveralls.phar --verbose

notifications:
  email: never
